services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: yishan
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - api-service-postgres:/var/lib/postgresql/data

volumes:
  api-service-postgres:
